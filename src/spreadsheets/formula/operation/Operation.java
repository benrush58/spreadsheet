package spreadsheets.formula.operation;

import spreadsheets.formula.Formula;

/**
 * Represents an operation in a worksheet.
 * An operation is a formula that can be evaluated.
 */
public interface Operation extends Formula {
}
